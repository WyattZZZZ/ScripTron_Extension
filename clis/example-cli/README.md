# example-cli

This is an example CLI template for ScripTron.

## Setup
Run the one-click install script to prepare the environment:
```bash
./install.sh
```

## Usage
The command is executed via `run.sh`:
```bash
./run.sh --input "Hello"
```
